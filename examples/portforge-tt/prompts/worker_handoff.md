<!-- Appended to every worker turn. Placeholders: {title}, {repo_id}, {key}. -->
FINAL WORKING PROTOCOL: A routine work unit covers up to four sequential short gates on unchanged
source (roughly six minutes). Aim to save your handoff within about eight minutes — that is a strong
target, not a hard rule: when a high-promise step is mid-flight and clearly progressing, finish it,
but first append a one-line progress checkpoint to STATE.md so an interrupted turn stays
recoverable. Never run past thirty minutes in one turn; the runtime cuts off beyond that. Stop early
on unexpected failure, drift or blockage; reconcile pending handles without duplicating work.
Preserve stage dependencies and every gate. The controller already saves complete requests, results
and logs. Keep STATE.md concise: current source, matrix status, pending handles, next step and
receipt paths. Keep prior archives intact, but do not recursively copy old notes, embed raw
receipts/logs, or print and retrieve a growing REPORT through device jobs. Use existing source-bound
evaluation receipts for routine identity checks; reserve extra device experiments for engineering
questions.
